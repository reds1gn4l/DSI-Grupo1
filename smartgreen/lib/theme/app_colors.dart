import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primária da marca
  static const green = Color(0xFF2E7D32);

  // Fundo padrão das telas
  static const background = Color(0xFFF7F7F7);

  // Tom clarinho dos cards/menus — mais escuro para melhorar o contraste
  // (se quiser ajustar, mexa aqui)
  static const surfaceAlt = Color(0xFFE9F2E5);

  // Superfície branca (cards brancos)
  static const surface = Colors.white;

  // Divisórias suaves
  static const divider = Color(0x1F000000);
}
