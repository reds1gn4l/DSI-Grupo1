import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primária da marca
  static const green = Color(0xFF2E7D32);

  // Ação/realce (botões azuis como “Adicionar” e “Visualizar no mapa”)
  static const blue = Color(0xFF1E88E5);

  // Fundo padrão das telas
  static const background = Color(0xFFF7F7F7);

  // Superfície clara de cards/menus com contraste melhor que o branco puro
  static const surfaceAlt = Color(0xFFE9F2E5);

  // Superfície branca (cards brancos)
  static const surface = Colors.white;

  // Divisórias suaves
  static const divider = Color(0x1F000000);
}
